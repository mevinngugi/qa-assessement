## Task A — Validate the BTC/ZAR market summary ##
Observations:
- The endpoint consistently returns a responses in less than 500 milliseconds, which is a good performance indicator for an API endpoint.
- The response has the same fields as documented on the API docs, which indicates that the API is functioning as expected and adhering to its documentation.
- The ask price is higher than the bid price.
